from re import split
from typing import Counter


def word_count(result):
    strg = result.split()
    c = len(strg)
    return c

def letter_count(result):
    strg = result.lower().split()
    c= []
    for letters in strg:
      letters.append(c)
    print(c)
    #print(strg,"this is letter_count")
    chars ={}
    count = 1
    for word in strg:
        if word not in chars:
            chars[word] = count
        elif word in chars:
            count =+1
            chars[word] = count +1 



